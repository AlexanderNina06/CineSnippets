@model List<Application.ViewModels.GeneroViewModel>

@{
    ViewData["Title"] = "Géneros";
}

<section>
    <div class="container">

        <div class="buttons is-right">
            <a asp-controller="Genero" asp-action="Create" class="button is-primary is-outlined is-responsive">Add gender</a>
        </div>


            @if (Model == null || Model.Count == 0)
            {
                <h2>No Genders Available</h2>
            }
            else
            {
            <div class="columns is-multiline">
                @foreach (Application.ViewModels.GeneroViewModel item in Model)
                {
                    <div class="column is-4-desktop is-6-tablet">
                        <div class="card">
                            <div class="card-content mt-4">
                                <div class="content">
                                    <div class="is-flex is-justify-content-center is-align-items-center">
                                        <small class="is-size-6 has-text-weight-semibold">@item.Nombre</small>
                                    </div>
                                </div>
                                <div class="card-footer">
                                    <div class="card-footer-item">
                                        <a asp-controller="Genero" asp-action="Edit" asp-route-id="@item.Id" class="button is-fullwidth has-text-grey-light is-uppercase" style="border: none;" data-config-id="primary-action1">Edit</a>
                                        <a asp-controller="Genero" asp-action="Delete" asp-route-id="@item.Id" class="button is-fullwidth has-text-grey-light is-uppercase" style="border: none;" data-config-id="primary-action1">Delete</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            }
    </div>
</section>